{
  "name": "sequelize-markup",
  "version": "1.0.6",
  "scripts": {
    "build": "babel src -d lib",
    "test": "mocha"
  },
  "devDependencies": {
    "babel-cli": "^6.0.0",
    "babel-preset-env": "^1.5.1",
    "mocha": "^3.4.2",
    "babel-template": "^6.24.1",
    "babel-types": "^6.24.1"
  },
  "main": "lib/sequelize-markup.js",
  "author": "Russ Panula <russ@dfa.net>",
  "license": "MIT",
  "description": "Babel plugin to transpile markup into sequelize models",
  "babel": {
    "presets": [
      [
        "env",
        {
          "targets": {
            "node": "current"
          }
        }
      ]
    ]
  },
  "repository": "dfadev/sequelize-markup"
}
